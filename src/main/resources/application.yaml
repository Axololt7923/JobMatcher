ai:
  service:
    url: http://localhost:8000
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/job-matcher
    username: ax0lotl79
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  security:
    user:
      password: minad

jwt:
  secret: "random-secret-key-tryingtomakeitlonger"
  expiration: 36000000 #10 hours
server:
  port: 8080

